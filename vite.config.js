// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  // IMPORTANT: Set this to your repository name exactly as it appears in the URL
  // Example: if your URL is anton9750.github.io/lawn-mower-project-three.js/
  // then base must be '/lawn-mower-project-three.js/'
  base: '/lawn-mower-project-three.js/',
});